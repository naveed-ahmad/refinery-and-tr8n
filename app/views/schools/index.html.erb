<h1><%= tr 'Listing schools' %></h1>

<table>
  <tr>
    <th><%= tr "Name" -%></th>
    <th><%= tr "Address"%></th>
    <th><%= tr "Phone" %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @schools.each do |school| %>
  <tr>
    <td><%= school.name %></td>
    <td><%= school.address %></td>
    <td><%= school.phone %></td>
    <td><%= link_to tr('Show'), school %></td>
    <td><%= link_to tr('Edit'), edit_school_path(school) %></td>
    <td><%= link_to tr('Destroy'), school, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to tr('New School'), new_school_path %>
